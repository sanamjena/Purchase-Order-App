<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <active>true</active>
    <description>Approved orders must not exceed budget when budget is provided.</description>
    <errorConditionFormula>AND(
        ISPICKVAL(Approval_Status__c, 'Approved'),
        NOT(ISBLANK(Budget_Amount__c)),
        BLANKVALUE(Total_Amount__c, 0) &gt; Budget_Amount__c
    )</errorConditionFormula>
    <errorMessage>Approved purchase orders cannot exceed their budget amount.</errorMessage>
</ValidationRule>
